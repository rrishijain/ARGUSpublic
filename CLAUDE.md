# ARGUS Student Edition

Read and follow `AGENTS.md` for the shared project instructions, then `ONBOARD.md` for first-time setup. Preserve the existing design unless the student asks for visual changes.

For background CLI requests, obey the supplied workflow response contract: Markdown for reports, structured JSON for conversation, and a fixed-SDK bundle for feature builds. Do not independently launch onboarding or invoke tools. The runner validates and saves the result; proposed actions and feature activation remain bounded local operations.
