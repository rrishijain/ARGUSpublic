Return {"manifest":{"name":"Short name","description":"What it does","kind":"panel" or "workspace","capabilities":[...],"sourceIds":[...],"schema":{"fieldName":{"type":"string"|"number"|"boolean"|"date","label":"Label","required":false}}},"source":"TSX source with export default function Feature() {...}","css":"local CSS","demoRecords":[records matching schema]}.
The only imports are React hooks from 'react' and {argus} from '@argus/sdk'. No other dependencies, network calls, eval, server code, external resources, HTML document or script tags. Render an accessible, responsive self-contained React component. Use the supplied DAYBREAK cream, charcoal and coral styling defaults. Prevent default form submission. Catch API errors and show them in the UI.
Capabilities are records.read, records.write, sources.read, tasks.read, tasks.write, notes.read, notes.write, reports.run. Select only needed capabilities. sourceIds may include only IDs explicitly present in CONTEXT.sources. New feature data is empty when activated; demonstration rows are shown only during preview. Label demonstrations as examples. Do not use demonstration rows as real business results. Do not invent metric values, assume live connections or treat period labels as actual date filtering.
argus.records.list(query?) resolves {rows,total,truncated,demonstration}; records.create(values), records.update(id,values) persist feature-owned records when active and edit disposable example records during preview. No delete API. Field names use letters/numbers/underscores and cannot be id, createdAt, updatedAt, constructor or prototype. New fields in later versions must be optional; existing fields/types must remain. Preview form edits are isolated from real data and reset when reopened. Active writes require the student to enable Allow local changes in the ARGUS host for this session.
argus.sources.query(sourceId,query?) returns rows or calculated value plus sourceId/capturedAt/demonstration. argus.sources.text(sourceId) returns {id,text,capturedAt,demonstration}. Query supports filters:[{column,operator:'eq'|'neq'|'contains'|'gt'|'gte'|'lt'|'lte',value}], orderBy:{column,direction:'asc'|'desc'}, limit:1..500, aggregation:'count'|'sum'|'average'|'latest',column. 'latest' returns the last row in explicit sort order. Aggregation uses all filtered rows before limit. Missing/non-numeric numeric inputs return value:null and an error. Use actual date filters for periods and state columns/units/period/source in metric labels. argus.calculate(rows,query) performs those calculations on provided rows; argus.ratio(numerator,denominator) returns {value,error}, null for zero denominator.
argus.tasks.list() returns {tasks,demonstration}; tasks.add(title); tasks.toggle(id,done). argus.notes.list() returns {notes,demonstration}; notes.add(title,text). argus.reports.run(workflow,question) queues one of the existing report workflows listed in the business context; do not auto-run reports. Writes and report generation must follow an explicit user button click. All methods return promises. Preview can edit only disposable feature records; tasks/notes are empty in preview and shared workspace writes/report requests are rejected. Include concise empty states and explain how to add real records or select sources.
